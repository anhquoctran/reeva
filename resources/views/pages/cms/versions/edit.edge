@cms.layout({ title: 'Edit Version' })
  <div class="max-w-4xl mx-auto space-y-8">
     <!-- Breadcrumbs -->
    <div class="flex items-center gap-2 text-[11px] font-bold text-gray-400 uppercase tracking-widest pl-1">
      <a href="{{ route('cms.versions.index') }}" class="hover:text-brand-600 transition-colors">Versions</a>
      <span>/</span>
      <span class="text-gray-900 italic tracking-widest uppercase">Edit Release</span>
    </div>

    <div>
      <h1 class="text-2xl font-bold text-gray-900 tracking-tight">Modify Version metadata</h1>
      <p class="text-gray-500 text-sm mt-1">Adjust metadata for version {{ version.major }}.{{ version.minor }}.{{ version.patch }}. Changes are reflected instantly.</p>
    </div>

    <div class="card-premium p-8 shadow-xl shadow-gray-100">
      @form({ action: route('cms.versions.update', { id: version.id }), method: 'POST' })
        <div class="space-y-8">
          
          <!-- Semantic Versioning Grid -->
          <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
            <div class="space-y-2">
               <label class="text-[10px] font-bold text-gray-450 uppercase tracking-widest">Major</label>
               <input type="number" name="major" value="{{ version.major }}" class="input-base text-lg font-bold py-3 text-center" required />
            </div>
            <div class="space-y-2">
               <label class="text-[10px] font-bold text-gray-450 uppercase tracking-widest">Minor</label>
               <input type="number" name="minor" value="{{ version.minor }}" class="input-base text-lg font-bold py-3 text-center" required />
            </div>
            <div class="space-y-2">
               <label class="text-[10px] font-bold text-gray-450 uppercase tracking-widest">Patch</label>
               <input type="number" name="patch" value="{{ version.patch }}" class="input-base text-lg font-bold py-3 text-center" required />
            </div>
             <div class="space-y-2">
               <label class="text-[10px] font-bold text-gray-450 uppercase tracking-widest">Is Visible?</label>
                <div class="flex items-center h-[54px] px-4 bg-gray-50 border border-gray-100 rounded-xl transition-colors hover:bg-white cursor-pointer group">
                  <input type="checkbox" name="isActive" class="w-5 h-5 rounded border-gray-300 text-brand-600 focus:ring-brand-500 cursor-pointer" {{ version.isActive ? 'checked' : '' }} />
                  <span class="ml-3 text-xs font-bold text-gray-500 group-hover:text-gray-900 transition-colors">Stable Release</span>
                </div>
            </div>
          </div>

          <!-- Metadata Grid -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6 pt-2 border-t border-gray-50">
             <div class="space-y-2">
               <label class="text-[10px] font-bold text-gray-450 uppercase tracking-widest tracking-widest">Release Codename</label>
               <input type="text" name="codename" value="{{ version.codename || '' }}" class="input-base font-medium py-3" />
            </div>
             <div class="space-y-2">
               <label class="text-[10px] font-bold text-gray-450 uppercase tracking-widest tracking-widest">Release Date</label>
               <input type="date" name="releaseDate" value="{{ version.releaseDate ? version.releaseDate.toISODate() : '' }}" class="input-base py-3" />
            </div>
          </div>

          <!-- Changelogs -->
          <div class="space-y-2 pt-2 border-t border-gray-50">
             <label class="text-[10px] font-bold text-gray-450 uppercase tracking-widest tracking-widest">Changelogs & Release Notes (Markdown Supported)</label>
             <div class="prose-editor">
               <textarea name="changelogs" id="changelogsEditor" rows="6">{{ version.changelog || '' }}</textarea>
             </div>
          </div>

          <!-- Actions -->
          <div class="pt-10 border-t border-gray-50 flex items-center justify-between">
             <a href="{{ route('cms.versions.index') }}" class="text-sm font-medium text-gray-450 hover:text-gray-900 transition-colors">Dismiss Changes</a>
             <button type="submit" class="btn-primary px-10 py-5 shadow-lg shadow-brand-100 rounded-2xl transform active:scale-95 duration-75">
               Apply Changes
             </button>
          </div>

        </div>
      @end
    </div>

  </div>

  <link rel="stylesheet" href="https://unpkg.com/easymde/dist/easymde.min.css">
  <style>
    .prose-editor .editor-toolbar { border-color: #f3f4f6; border-radius: 1rem 1rem 0 0; background-color: #f9fafb; opacity: 0.9; padding: 0.5rem; }
    .prose-editor .editor-toolbar button { color: #6b7280; border-radius: 0.5rem; transition: all 0.2s; }
    .prose-editor .editor-toolbar button.active, .prose-editor .editor-toolbar button:hover { background-color: #e5e7eb; border-color: transparent; }
    .prose-editor .CodeMirror { border-color: #f3f4f6; border-radius: 0 0 1rem 1rem; font-family: 'Inter', sans-serif; font-size: 0.875rem; min-height: 200px; padding: 1rem; color: #111827; }
    .prose-editor .CodeMirror-focused { border-color: #6366f1; box-shadow: 0 0 0 2px rgba(99, 102, 241, 0.1); }
  </style>
  <script src="https://unpkg.com/easymde/dist/easymde.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      new EasyMDE({ 
        element: document.getElementById('changelogsEditor'),
        spellChecker: false,
        status: false,
        toolbar: ["bold", "italic", "heading", "|", "quote", "unordered-list", "ordered-list", "|", "link", "code", "preview"]
      });
    });
  </script>
@end
